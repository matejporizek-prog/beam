# Copy this file to ".env" and paste your key in. The .env file is gitignored,
# so your key never gets committed to the public repo.
#
# Get either value from https://www.themoviedb.org/settings/api
# You only need ONE of them.

TMDB_API_KEY=
# TMDB_READ_TOKEN=
