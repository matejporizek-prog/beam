# Normalise line endings so the repo is consistent across machines. This keeps
# the scripts inside .github workflows as LF, which is what the Linux CI runner
# needs — CRLF there can break bash steps.
* text=auto eol=lf

# Binary assets — never touch these.
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
*.webp binary
